#include<stdio.h>
int main()
{
	int n,flag=0;
	printf("enter 2 no.");
	scanf("%d",&n);
	if(n==1||n==0)
	printf("no. is not prime");
	for(int i=1;i<=n;i++)
	{
		if(n%i=0)
		flag++;
	}
	if(flag==2)
	printf("no. is prime");
	else
	printf("no. is not prime");
	
	
	
  return 0;
	
}
