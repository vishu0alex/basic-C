#include<stdio.h>
int main()
{
	float a=1.24,b=2.64,r,s,t,c;
	s=a*a;
	t=a*b/2;
	r=a*b;
	c=22*a/7;
	printf("area of square= %.2f\n",s);
	printf("area of triangle= %.2f\n",t);
	printf("area of rectangle= %.2f\n",r);
	printf("area of circle= %.2f\n",c);
	
	
	
  return 0;
	
}
